extends ../layouts/application.pug

block header
  = t('views.labels.new.header')

block content
  +formFor()(action='/labels' method='post')
    +input(label, 'name', errors)(type="text" placeholder=t('views.label'))
    +submit(label)(value=t('views.create'))